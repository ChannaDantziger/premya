# מסמך אפיון – מערכת לניהול שיטות פרמיה

## תוכן עניינים

1. מטרת המערכת והיקף
2. דרישות פונקציונליות
3. תהליך העבודה המרכזי
4. מבנה המסכים
5. לוגיקה עסקית
6. תכנון מבנה הנתונים הדינמי
7. תיאור המבנה הטבלאי
8. ארכיטקטורת המערכת
9. תיאור שכבות המערכת
10. תכנון API
11. SQL ושליפות מרכזיות
12. חלופות לפתרון המידע הלא הומוגני
13. תרחיש שינוי מבנה Excel
14. טכנולוגיות
15. תשובות לשאלות הנוספות

## 1. מטרת המערכת והיקף

### 1.1 מטרת המערכת

המערכת מיועדת לאפשר ניהול של שיטות פרמיה ומדדים עסקיים, וכן קליטת נתונים מקובצי Excel בעלי מבנים שונים ולא אחידים.

המערכת תאפשר למשתמש:

- להגדיר שיטות פרמיה.
- להגדיר מדדים עבור כל שיטה.
- להגדיר אילו שדות רלוונטיים לכל מדד.
- לקלוט קובצי Excel עבור מדדים.
- לשמור את הנתונים באופן גנרי ודינמי.
- לשמור היסטוריה של הקבצים והנתונים שנקלטו.
- לצפות בנתונים שנקלטו באמצעות טבלה דינמית.
- לסנן ולמיין את הנתונים.

### 1.2 תחום המערכת

במסגרת המערכת יפותחו:

- מסך לניהול שיטות פרמיה.
- מסך לניהול מדדים.
- מסך לקליטת קובץ Excel ולצפייה בנתונים.
- Web API לניהול הנתונים ולביצוע הקליטה והשליפות.
- מסד נתונים לשמירת ההגדרות, מבני הקבצים, היסטוריית הקליטות והנתונים.

בשלב זה המערכת מתמקדת בקליטת מידע מקובצי Excel בלבד, למרות שהמודל מאפשר בעתיד תמיכה במקורות מידע נוספים.

### 1.3 משתמשי המערכת

המערכת מיועדת למשתמש עסקי או תפעולי האחראי על ניהול שיטות הפרמיה וקליטת נתוני המדדים.

המשתמש יוכל:

- להגדיר ולעדכן שיטות פרמיה.
- להגדיר ולעדכן מדדים.
- להגדיר את השדות הרלוונטיים לכל מדד.
- להעלות קובצי Excel.
- לצפות בהיסטוריית הקליטות.
- לצפות, לסנן ולמיין את הנתונים שנקלטו.

לא מוגדרת בשלב זה חלוקה לתפקידים או מערכת הרשאות, מאחר שדרישה כזו אינה מופיעה במבחן.

## 2. דרישות פונקציונליות

### 2.1 ניהול שיטות פרמיה

המערכת תאפשר להציג, להוסיף ולעדכן שיטות פרמיה, הכוללות מספר שיטה, תיאור, אחוז פרמיה ותקופת חישוב.

### 2.2 ניהול מדדים

המערכת תאפשר להציג, להוסיף ולעדכן מדדים המשויכים לשיטת פרמיה, כולל פרטי המדד, מקור המידע, תדירות הקליטה והשדות הרלוונטיים.

### 2.3 קליטת קובצי Excel

המערכת תאפשר למשתמש לבחור מדד, שנה ותקופת חישוב, להעלות קובץ Excel, לזהות את מבנה הקובץ ולקלוט את הנתונים.

### 2.4 ניהול היסטוריית קליטות

המערכת תשמור ותציג היסטוריה של הקבצים שנקלטו, כולל המדד, התקופה, מועד הקליטה, סטטוס הקליטה ומספר הרשומות שנקלטו.

### 2.5 שליפת נתונים והצגתם

המערכת תאפשר לצפות בנתונים שנקלטו עבור כל מדד, באמצעות מבנה תצוגה דינמי בהתאם לשדות הקובץ.

### 2.6 סינון, מיון ותמיכה בכמויות מידע גדולות

המערכת תאפשר לסנן ולמיין את הנתונים לפי שדות רלוונטיים, תוך תמיכה בשליפת כמויות מידע גדולות.

## 3. תהליך העבודה המרכזי

תהליך העבודה המרכזי במערכת הוא כדלקמן:

1. המשתמש יוצר שיטת פרמיה ומגדיר את פרטיה.
2. המשתמש מוסיף מדד ומשייך אותו לשיטת הפרמיה.
3. המשתמש מגדיר את מקור המידע ואת תדירות הקליטה.
4. המשתמש מגדיר אילו שדות רלוונטיים למדד.
5. המשתמש בוחר מדד, שנה ותקופת חישוב.
6. המשתמש מעלה קובץ Excel.
7. המערכת בודקת את מבנה הקובץ ואת הנתונים שבו.
8. המערכת מזהה את השדות בקובץ ומשווה אותם להגדרת המדד.
9. המערכת שומרת את מבנה הקובץ ואת נתוני הקליטה.
10. המערכת שומרת רשומה בהיסטוריית הקליטות.
11. המערכת מציגה למשתמש את תוצאת הקליטה.
12. המשתמש צופה בנתונים, מסנן וממיין אותם.

במקרה של שגיאה, הקליטה תסומן ככושלת ותוצג למשתמש סיבת הכשל.

## 4. מבנה המסכים

### 4.1 מסך 1 – ניהול שיטות פרמיה

**מטרה:** להציג ולנהל את שיטות הפרמיה במערכת.

**פעולות עיקריות:**

- הצגת רשימת שיטות פרמיה.
- הוספת שיטת פרמיה.
- עריכת שיטת פרמיה.
- צפייה בפרטי שיטת פרמיה ובמדדים המשויכים אליה.

**שדות עיקריים:**

- מספר שיטה.
- תיאור שיטה.
- אחוז פרמיה.
- תקופת חישוב.

### 4.2 מסך 2 – ניהול מדדים

**מטרה:** לנהל את המדדים המשויכים לשיטת פרמיה.

**פעולות עיקריות:**

- בחירת שיטת פרמיה.
- הצגת רשימת המדדים של שיטת הפרמיה.
- הוספת מדד.
- עריכת מדד.
- הגדרת מקור המידע.
- הגדרת השדות הרלוונטיים למדד.

**שדות עיקריים:**

- שם המדד.
- תיאור המדד.
- סוג מקור המידע.
- שם מקור המידע.
- תדירות קליטת המידע.
- רשימת השדות הרלוונטיים למדד.

### 4.3 מסך 3 – קליטת נתונים וצפייה בנתונים

**מטרה:** קליטת קובץ Excel עבור מדד וצפייה בנתונים שנקלטו בפועל.

**בחירת נתונים:**

- בחירת שיטת פרמיה.
- בחירת מדד.
- בחירת שנה ותקופת חישוב.
- בחירת קובץ Excel.

**פעולות עיקריות:**

- העלאת קובץ Excel.
- זיהוי מבנה הקובץ והשדות שבו.
- הצגת השדות שזוהו.
- בחירת השדות הרלוונטיים למדד, במקרה שנדרש.
- אישור הקליטה.
- הצגת תוצאת הקליטה.
- הצגת היסטוריית הקליטות עבור המדד.
- צפייה בנתונים שנקלטו.

**תצוגת הנתונים:**

- טבלה דינמית בהתאם למבנה המדד והקובץ שנקלט.
- כותרות עמודות הנקבעות לפי השדות שנשמרו.
- הצגת ערכים בהתאם לסוג הנתון.
- סינון לפי שדות זמינים.
- מיון לפי שדות זמינים.
- תמיכה בשליפת כמויות מידע גדולות באמצעות טעינה מדורגת או עימוד.

**הודעות למשתמש:**

- קובץ נקלט בהצלחה.
- מספר הרשומות שנקלטו.
- שדות חסרים או לא תקינים.
- שגיאות במבנה הקובץ או בנתונים.
- כשל בקליטה ותיאור סיבת הכשל.

## 5. לוגיקה עסקית

### 5.1 כללים עבור שיטות פרמיה

- לכל שיטת פרמיה יהיה מספר מזהה.
- לכל שיטה יוגדרו תיאור, אחוז פרמיה ותקופת חישוב.
- מספר שיטה לא יוכל להיות כפול.

### 5.2 כללים עבור מדדים

- כל מדד יהיה משויך לשיטת פרמיה.
- לכל מדד יוגדר שם, תיאור, מקור מידע ותדירות קליטה.
- לכל מדד תוגדר רשימת השדות הרלוונטיים עבורו.
- מדד חדש לא ידרוש שינוי בסכמת בסיס הנתונים.

### 5.3 כללים עבור מבנה קובץ Excel

- הקובץ חייב להיות קובץ Excel תקין.
- המערכת תזהה את שמות השדות ואת סוגי הנתונים.
- המערכת תאפשר התמודדות עם מספר שדות משתנה.
- שדות שאינם רלוונטיים למדד לא ייכללו בתצוגת המדד.
- מבנה קובץ חדש יישמר כגרסה חדשה, ולא ידרוס מבנה קודם.

### 5.4 כללים עבור קליטת נתונים

- כל קליטה תשויך למדד, לשנה ולתקופת חישוב.
- לפני שמירת הנתונים תתבצע בדיקה של מבנה הקובץ והנתונים.
- במקרה של קליטה מוצלחת יישמרו הנתונים והיסטוריית הקליטה.
- במקרה של שגיאה הקליטה תסומן ככושלת ותישמר סיבת הכשל.
- נתונים מקליטות קודמות לא יימחקו בעת קליטת קובץ חדש.

### 5.5 כללים עבור היסטוריית קליטות

- לכל קליטה יישמרו פרטי הקובץ, המדד, התקופה, מועד הקליטה והסטטוס.
- ניתן יהיה לצפות בנתונים בהתאם לקליטה שנבחרה.
- ההיסטוריה תאפשר להבחין בין מבני קבצים ותקופות שונות.

## 6. תכנון מבנה הנתונים הדינמי

### 6.1 הבעיה העסקית

לכל מדד יכול להיות מבנה Excel שונה. בנוסף, מבנה של קובץ קיים עשוי להשתנות בעתיד. לכן לא ניתן להסתמך על טבלה קבועה עם עמודה לכל שדה עסקי.

### 6.2 הפתרון שנבחר

המערכת תשמור את מבנה הקובץ בנפרד מהנתונים עצמם:

- לכל מדד יישמר מבנה קובץ.
- כל שינוי במבנה יישמר כגרסה חדשה.
- עבור כל שדה יישמרו שם השדה, סוג הנתון, האם השדה רלוונטי למדד וסדר ההצגה.
- הנתונים יישמרו באופן גנרי, ללא צורך בעמודה קבועה לכל שדה אפשרי.
- כל רשומת נתונים תקושר למדד, לגרסת המבנה ולקליטה שבה נקלטה.

### 6.3 שמירת מבנה הקובץ וגרסאותיו

בעת קליטת קובץ:

1. המערכת תזהה את שמות השדות.
2. המערכת תזהה את סוגי הנתונים.
3. המערכת תשווה את המבנה למבנים קודמים של אותו מדד.
4. אם המבנה השתנה, תיווצר גרסה חדשה.
5. המבנה הקודם יישמר לצורך היסטוריה ותאימות לנתונים ישנים.

### 6.4 שמירת הנתונים

הנתונים יישמרו בצורה גנרית ויקושרו למדד, לגרסת מבנה הקובץ ולהיסטוריית הקליטה.

### 6.5 הגדרת שדות רלוונטיים לכל מדד

המערכת תאפשר להגדיר אילו שדות מתוך קובץ Excel רלוונטיים למדד. שדות שאינם רלוונטיים עדיין יכולים להישמר כחלק ממבנה הקובץ, אך לא יוצגו בתצוגה המרכזית של המדד.

### 6.6 שליפה והצגה דינמית

בעת הצגת נתוני מדד:

- המערכת תשלוף את מבנה השדות המתאים לקליטה.
- תיבנה טבלה דינמית בהתאם לשדות.
- יוצגו רק השדות הרלוונטיים.
- ניתן יהיה לבצע סינון ומיון לפי השדות הזמינים.

## 7. תיאור המבנה הטבלאי

### 7.1 ישויות מרכזיות

1. **PremiumMethods – שיטות פרמיה**
   - מזהה.
   - מספר שיטה.
   - תיאור.
   - אחוז פרמיה.
   - תקופת חישוב.

2. **Metrics – מדדים**
   - מזהה.
   - מזהה שיטת פרמיה.
   - שם המדד.
   - תיאור.
   - סוג מקור המידע.
   - שם מקור המידע.
   - תדירות קליטה.

3. **MetricFields – שדות מדד**
   - מזהה.
   - מזהה מדד.
   - שם השדה.
   - סוג הנתון.
   - האם השדה רלוונטי.
   - סדר הצגה.

4. **FileStructureVersions – גרסאות מבנה קובץ**
   - מזהה.
   - מזהה מדד.
   - מספר גרסה.
   - תאריך יצירה.
   - סימון האם זו הגרסה הפעילה.

5. **ImportBatches – היסטוריית קליטות**
   - מזהה.
   - מזהה מדד.
   - מזהה גרסת מבנה.
   - שם הקובץ.
   - שנה.
   - תקופת חישוב.
   - מועד הקליטה.
   - סטטוס.
   - הודעת שגיאה, אם קיימת.
   - מספר רשומות שנקלטו.

6. **DynamicRecords – רשומות נתונים**
   - מזהה.
   - מזהה קליטה.
   - מספר שורה בקובץ.

7. **DynamicValues – ערכים דינמיים**
   - מזהה.
   - מזהה רשומה.
   - מזהה שדה.
   - ערך טקסטואלי, מספרי, תאריך או בוליאני.

#### 7.1.1 טבלת PremiumMethods

טבלה זו תשמור את שיטות הפרמיה ותשמש כטבלת האב של המדדים.

| עמודה | סוג ב־SQLite | אילוצים | תיאור |
|---|---|---|---|
| `Id` | `INTEGER` | `PRIMARY KEY AUTOINCREMENT` | מזהה פנימי של השיטה |
| `MethodNumber` | `TEXT` | `NOT NULL UNIQUE` | מספר השיטה העסקי |
| `Description` | `TEXT` | `NOT NULL` | תיאור שיטת הפרמיה |
| `PremiumRate` | `NUMERIC` | `NOT NULL`, בין 0 ל־100 | אחוז הפרמיה |
| `CalculationPeriod` | `TEXT` | `NOT NULL` | תקופת החישוב, למשל חודש או רבעון |

`MethodNumber` נשמר כ־`TEXT` כדי לאפשר מספרים עם אפסים מובילים או ערכים כגון `PM-001`.

#### 7.1.2 SQL ליצירת הטבלה

```sql
CREATE TABLE PremiumMethods (
    Id INTEGER PRIMARY KEY AUTOINCREMENT,
    MethodNumber TEXT NOT NULL UNIQUE,
    Description TEXT NOT NULL,
    PremiumRate NUMERIC NOT NULL
        CHECK (PremiumRate >= 0 AND PremiumRate <= 100),
    CalculationPeriod TEXT NOT NULL
);
```

#### 7.1.3 טבלת Metrics

הטבלה תשמור את המדדים ותשייך כל מדד לשיטת פרמיה אחת.

| עמודה | סוג ב־SQLite | אילוצים | תיאור |
|---|---|---|---|
| `Id` | `INTEGER` | `PRIMARY KEY AUTOINCREMENT` | מזהה פנימי |
| `PremiumMethodId` | `INTEGER` | `NOT NULL`, מפתח זר | שיטת הפרמיה שאליה שייך המדד |
| `Name` | `TEXT` | `NOT NULL` | שם המדד |
| `Description` | `TEXT` | `NOT NULL` | תיאור המדד |
| `SourceType` | `TEXT` | `NOT NULL` | סוג מקור המידע |
| `SourceName` | `TEXT` | `NOT NULL` | שם מקור המידע |
| `IngestionFrequency` | `TEXT` | `NOT NULL` | תדירות קליטת המידע |

אילוצים:

- כל מדד חייב להשתייך לשיטת פרמיה.
- לא ניתן ליצור שני מדדים בעלי אותו שם תחת אותה שיטת פרמיה.
- סוג מקור המידע יהיה `Excel` או `ExternalInterface`.
- בשלב הנוכחי בפועל נתמקד ב־`Excel`.

#### 7.1.4 SQL ליצירת טבלת Metrics

```sql
CREATE TABLE Metrics (
    Id INTEGER PRIMARY KEY AUTOINCREMENT,
    PremiumMethodId INTEGER NOT NULL,
    Name TEXT NOT NULL,
    Description TEXT NOT NULL,
    SourceType TEXT NOT NULL,
    SourceName TEXT NOT NULL,
    IngestionFrequency TEXT NOT NULL,

    CONSTRAINT FK_Metrics_PremiumMethods
        FOREIGN KEY (PremiumMethodId)
        REFERENCES PremiumMethods(Id),

    CONSTRAINT UQ_Metrics_Method_Name
        UNIQUE (PremiumMethodId, Name),

    CONSTRAINT CK_Metrics_SourceType
        CHECK (SourceType IN ('Excel', 'ExternalInterface'))
);
```

#### 7.1.5 טבלת MetricFields

הטבלה מגדירה את השדות הלוגיים שהמדד מכיר ואת אופן הצגתם.

| עמודה | סוג ב־SQLite | אילוצים | תיאור |
|---|---|---|---|
| `Id` | `INTEGER` | `PRIMARY KEY AUTOINCREMENT` | מזהה פנימי |
| `MetricId` | `INTEGER` | `NOT NULL`, מפתח זר | המדד שאליו שייך השדה |
| `FieldName` | `TEXT` | `NOT NULL` | שם השדה |
| `DataType` | `TEXT` | `NOT NULL` | סוג הנתון |
| `IsRelevant` | `INTEGER` | `NOT NULL`, 0 או 1 | האם השדה רלוונטי למדד |
| `DisplayOrder` | `INTEGER` | `NOT NULL`, גדול או שווה ל־0 | סדר הצגת השדה |

```sql
CREATE TABLE MetricFields (
    Id INTEGER PRIMARY KEY AUTOINCREMENT,
    MetricId INTEGER NOT NULL,
    FieldName TEXT NOT NULL,
    DataType TEXT NOT NULL,
    IsRelevant INTEGER NOT NULL DEFAULT 1,
    DisplayOrder INTEGER NOT NULL DEFAULT 0,

    CONSTRAINT FK_MetricFields_Metrics
        FOREIGN KEY (MetricId)
        REFERENCES Metrics(Id),

    CONSTRAINT UQ_MetricFields_Metric_Name
        UNIQUE (MetricId, FieldName),

    CONSTRAINT CK_MetricFields_DataType
        CHECK (DataType IN ('Text', 'Integer', 'Decimal', 'Date', 'Boolean')),

    CONSTRAINT CK_MetricFields_IsRelevant
        CHECK (IsRelevant IN (0, 1)),

    CONSTRAINT CK_MetricFields_DisplayOrder
        CHECK (DisplayOrder >= 0)
);
```

#### 7.1.6 טבלת FileStructureVersions

הטבלה שומרת גרסאות שונות של מבנה קובץ עבור מדד.

| עמודה | סוג ב־SQLite | אילוצים | תיאור |
|---|---|---|---|
| `Id` | `INTEGER` | `PRIMARY KEY AUTOINCREMENT` | מזהה פנימי |
| `MetricId` | `INTEGER` | `NOT NULL`, מפתח זר | המדד שאליו שייכת הגרסה |
| `VersionNumber` | `INTEGER` | `NOT NULL, גדול מ־0` | מספר גרסה |
| `CreatedAt` | `TEXT` | `NOT NULL` | מועד יצירת הגרסה |
| `IsActive` | `INTEGER` | `NOT NULL`, 0 או 1 | האם הגרסה פעילה |

```sql
CREATE TABLE FileStructureVersions (
    Id INTEGER PRIMARY KEY AUTOINCREMENT,
    MetricId INTEGER NOT NULL,
    VersionNumber INTEGER NOT NULL,
    CreatedAt TEXT NOT NULL,
    IsActive INTEGER NOT NULL DEFAULT 1,

    CONSTRAINT FK_FileStructureVersions_Metrics
        FOREIGN KEY (MetricId)
        REFERENCES Metrics(Id),

    CONSTRAINT UQ_FileStructureVersions_Metric_Version
        UNIQUE (MetricId, VersionNumber),

    CONSTRAINT CK_FileStructureVersions_Version
        CHECK (VersionNumber > 0),

    CONSTRAINT CK_FileStructureVersions_IsActive
        CHECK (IsActive IN (0, 1))
);
```

#### 7.1.7 טבלת FileStructureFields

הטבלה שומרת את השדות שהופיעו בפועל בגרסת קובץ מסוימת. `MetricFieldId` יכול להיות ריק כאשר מדובר בשדה חדש שטרם הוגדר כשדה לוגי במדד.

| עמודה | סוג ב־SQLite | אילוצים | תיאור |
|---|---|---|---|
| `Id` | `INTEGER` | `PRIMARY KEY AUTOINCREMENT` | מזהה פנימי |
| `FileStructureVersionId` | `INTEGER` | `NOT NULL`, מפתח זר | גרסת המבנה |
| `MetricFieldId` | `INTEGER` | מפתח זר, ניתן לריק | השדה הלוגי המתאים |
| `FieldName` | `TEXT` | `NOT NULL` | שם השדה בקובץ |
| `DataType` | `TEXT` | `NOT NULL` | סוג הנתון |
| `IsRelevant` | `INTEGER` | `NOT NULL`, 0 או 1 | האם יוצג במדד |
| `DisplayOrder` | `INTEGER` | `NOT NULL`, גדול או שווה ל־0 | סדר הצגה |

```sql
CREATE TABLE FileStructureFields (
    Id INTEGER PRIMARY KEY AUTOINCREMENT,
    FileStructureVersionId INTEGER NOT NULL,
    MetricFieldId INTEGER,
    FieldName TEXT NOT NULL,
    DataType TEXT NOT NULL,
    IsRelevant INTEGER NOT NULL DEFAULT 1,
    DisplayOrder INTEGER NOT NULL DEFAULT 0,

    CONSTRAINT FK_FileStructureFields_Versions
        FOREIGN KEY (FileStructureVersionId)
        REFERENCES FileStructureVersions(Id),

    CONSTRAINT FK_FileStructureFields_MetricFields
        FOREIGN KEY (MetricFieldId)
        REFERENCES MetricFields(Id),

    CONSTRAINT UQ_FileStructureFields_Version_Name
        UNIQUE (FileStructureVersionId, FieldName),

    CONSTRAINT CK_FileStructureFields_DataType
        CHECK (DataType IN ('Text', 'Integer', 'Decimal', 'Date', 'Boolean')),

    CONSTRAINT CK_FileStructureFields_IsRelevant
        CHECK (IsRelevant IN (0, 1)),

    CONSTRAINT CK_FileStructureFields_DisplayOrder
        CHECK (DisplayOrder >= 0)
);
```

#### 7.1.8 טבלת ImportBatches

הטבלה שומרת את היסטוריית קליטת הקבצים.

```sql
CREATE TABLE ImportBatches (
    Id INTEGER PRIMARY KEY AUTOINCREMENT,
    MetricId INTEGER NOT NULL,
    FileStructureVersionId INTEGER NOT NULL,
    FileName TEXT NOT NULL,
    DataYear INTEGER NOT NULL,
    CalculationPeriod TEXT NOT NULL,
    ImportedAt TEXT NOT NULL,
    Status TEXT NOT NULL,
    ErrorMessage TEXT,
    RecordCount INTEGER NOT NULL DEFAULT 0,

    CONSTRAINT FK_ImportBatches_Metrics
        FOREIGN KEY (MetricId)
        REFERENCES Metrics(Id),

    CONSTRAINT FK_ImportBatches_StructureVersions
        FOREIGN KEY (FileStructureVersionId)
        REFERENCES FileStructureVersions(Id),

    CONSTRAINT CK_ImportBatches_Status
        CHECK (Status IN ('Pending', 'Succeeded', 'Failed')),

    CONSTRAINT CK_ImportBatches_RecordCount
        CHECK (RecordCount >= 0)
);
```

#### 7.1.9 טבלת DynamicRecords

הטבלה שומרת את הרשומות שנקלטו בפועל. המדד וגרסת המבנה ניתנים לגזירה דרך `ImportBatches`, ולכן אינם נשמרים שוב בטבלה זו.

```sql
CREATE TABLE DynamicRecords (
    Id INTEGER PRIMARY KEY AUTOINCREMENT,
    ImportBatchId INTEGER NOT NULL,
    RowNumber INTEGER NOT NULL,

    CONSTRAINT FK_DynamicRecords_ImportBatches
        FOREIGN KEY (ImportBatchId)
        REFERENCES ImportBatches(Id),

    CONSTRAINT UQ_DynamicRecords_Batch_Row
        UNIQUE (ImportBatchId, RowNumber),

    CONSTRAINT CK_DynamicRecords_RowNumber
        CHECK (RowNumber > 0)
);
```

#### 7.1.10 טבלת DynamicValues

הטבלה שומרת את הערכים הדינמיים של כל רשומה. נשמרת עמודת ערך מתאימה לפי סוג הנתון, כדי לאפשר מיון וסינון נכונים.

```sql
CREATE TABLE DynamicValues (
    Id INTEGER PRIMARY KEY AUTOINCREMENT,
    DynamicRecordId INTEGER NOT NULL,
    FileStructureFieldId INTEGER NOT NULL,
    ValueText TEXT,
    ValueNumber NUMERIC,
    ValueDate TEXT,
    ValueBoolean INTEGER,

    CONSTRAINT FK_DynamicValues_Records
        FOREIGN KEY (DynamicRecordId)
        REFERENCES DynamicRecords(Id),

    CONSTRAINT FK_DynamicValues_Fields
        FOREIGN KEY (FileStructureFieldId)
        REFERENCES FileStructureFields(Id),

    CONSTRAINT UQ_DynamicValues_Record_Field
        UNIQUE (DynamicRecordId, FileStructureFieldId),

    CONSTRAINT CK_DynamicValues_Boolean
        CHECK (ValueBoolean IS NULL OR ValueBoolean IN (0, 1))
);
```

### 7.2 קשרים בין הישויות

- `PremiumMethods` 1:N `Metrics`.
- `Metrics` 1:N `MetricFields`.
- `Metrics` 1:N `FileStructureVersions`.
- `FileStructureVersions` 1:N `FileStructureFields`.
- `Metrics` 1:N `ImportBatches`.
- `FileStructureVersions` 1:N `ImportBatches`.
- `ImportBatches` 1:N `DynamicRecords`.
- `DynamicRecords` 1:N `DynamicValues`.
- `FileStructureFields` 1:N `DynamicValues`.

### 7.3 מפתחות ואילוצים

- לכל טבלה יהיה מפתח ראשי.
- קשרים בין הטבלאות ימומשו באמצעות מפתחות זרים.
- מספר שיטת פרמיה יהיה ייחודי.
- שם מדד יהיה ייחודי במסגרת שיטת פרמיה.
- שמות שדות יהיו ייחודיים במסגרת גרסת מבנה.
- לא ניתן למחוק שיטת פרמיה שיש לה מדדים או נתונים.

### 7.4 אינדקסים מרכזיים

- אינדקס על מספר שיטת הפרמיה.
- אינדקס על שיטת פרמיה במדדים.
- אינדקס על מדד בגרסאות המבנה.
- אינדקס על מדד והתקופה בהיסטוריית הקליטות.
- אינדקס על מזהה קליטה ברשומות הנתונים.
- אינדקס על מזהה שדה בערכי הנתונים.

```sql
CREATE INDEX IX_Metrics_PremiumMethodId
    ON Metrics(PremiumMethodId);

CREATE INDEX IX_MetricFields_MetricId
    ON MetricFields(MetricId);

CREATE INDEX IX_FileStructureVersions_MetricId
    ON FileStructureVersions(MetricId);

CREATE INDEX IX_FileStructureFields_VersionId
    ON FileStructureFields(FileStructureVersionId);

CREATE INDEX IX_ImportBatches_Metric_Period
    ON ImportBatches(MetricId, DataYear, CalculationPeriod);

CREATE INDEX IX_DynamicRecords_ImportBatchId
    ON DynamicRecords(ImportBatchId);

CREATE INDEX IX_DynamicValues_RecordId
    ON DynamicValues(DynamicRecordId);

CREATE INDEX IX_DynamicValues_FieldId
    ON DynamicValues(FileStructureFieldId);
```

סדר יצירת הטבלאות:

```text
1. PremiumMethods
2. Metrics
3. MetricFields
4. FileStructureVersions
5. FileStructureFields
6. ImportBatches
7. DynamicRecords
8. DynamicValues
9. Indexes
```

לפני עבודה עם קשרי מפתח זר ב־SQLite יש להפעיל:

```sql
PRAGMA foreign_keys = ON;
```

### 7.5 תרשים ERD

בסעיף זה יופיע תרשים המתאר את הטבלאות והקשרים ביניהן.

## 8. ארכיטקטורת המערכת

### 8.1 עקרונות ארכיטקטוניים

המערכת תהיה מחולקת לשכבות, כאשר לכל שכבה אחריות ברורה. מטרת החלוקה היא להפריד בין ממשק המשתמש, הלוגיקה העסקית וגישה למסד הנתונים, ולאפשר תחזוקה והרחבה נוחות.

### 8.2 חלוקה לשכבות

המערכת תכלול את השכבות הבאות:

- **Angular Client** – ממשק המשתמש.
- **Web API** – קבלת בקשות HTTP והחזרת תשובות.
- **Application / Business** – ביצוע תהליכים ולוגיקה עסקית.
- **Domain** – מודלים וכללים מרכזיים של המערכת.
- **Data Access / Infrastructure** – גישה למסד הנתונים ולשירותים חיצוניים.
- **Excel Import** – קריאת קובץ Excel, זיהוי המבנה והמרת הנתונים למבנה גנרי.

### 8.3 פירוק רכיבי המערכת לפי אחריות

הרכיבים יחולקו לפי תחומי אחריות:

- ניהול שיטות פרמיה.
- ניהול מדדים ושדות.
- זיהוי מבנה קובץ.
- קליטת נתונים.
- ניהול היסטוריית קליטות.
- שליפת נתונים דינמיים.
- סינון ומיון נתונים.

### 8.4 זרימת מידע בין השכבות

הזרימה המרכזית תהיה:

```text
Angular
   ↓
Web API
   ↓
Application / Business
   ↓
Domain + Excel Import
   ↓
Data Access
   ↓
SQLite
```

בעת קליטת קובץ, ה־API יקבל את הקובץ והפרטים הנלווים, שכבת ה־Application תפעיל את תהליך הקליטה, רכיב Excel Import יזהה את המבנה, ולבסוף הנתונים יישמרו דרך שכבת הגישה למסד הנתונים.

### 8.5 שיקולי סקיילביליות

- הפרדת הקליטה והשליפה לרכיבים נפרדים.
- שימוש בעימוד בעת שליפת נתונים רבים.
- ביצוע סינון ומיון בצד השרת.
- שימוש באינדקסים בהתאם לשאילתות המרכזיות.
- שמירת מבנה הנתונים באופן גנרי, כדי להוסיף מדדים ללא שינוי משמעותי ב־Database.
- אפשרות להחליף את SQLite ב־SQL Server בעתיד ללא שינוי משמעותי בשכבות האחרות.

### 8.6 ארכיטקטורת צד הלקוח

צד הלקוח יחולק לפי תחומי אחריות ותכונות מערכת:

```text
src/app
├── core
│   ├── services
│   ├── interfaces
│   └── models
├── shared
│   └── components
├── features
│   ├── premium-methods
│   ├── metrics
│   └── metric-data
├── app.routes.ts
└── app.config.ts
```

- `core` – רכיבים ושירותים כלליים של האפליקציה, כגון שירותי API, Interfaces ומודלים משותפים.
- `shared` – רכיבי UI כלליים שניתן להשתמש בהם במספר מסכים.
- `features` – חלוקה לפי תחומי המערכת: ניהול שיטות פרמיה, ניהול מדדים וקליטת נתונים וצפייה בנתונים.
- `app.routes.ts` – הגדרת הניווט בין המסכים.
- `app.config.ts` – הגדרות האפליקציה והזרקת התלויות.

כל Feature יכיל את הרכיבים, הטפסים והשירותים הייחודיים לו. רכיבים משותפים, כגון טבלה דינמית או רכיב סינון, יוגדרו ב־`shared`.

אין צורך בספריית State Management חיצונית. ניהול המידע יתבצע באמצעות שירותי Angular, טפסים ונתונים המתקבלים מה־API.

## 9. תיאור שכבות המערכת

### 9.1 Angular – שכבת ממשק המשתמש

אחראית על הצגת המסכים, קבלת קלט מהמשתמש, ניהול טפסים באמצעות Reactive Forms, שליחת בקשות ל־API, הצגת נתונים, הודעות שגיאה ותוצאות קליטה, והצגת טבלה דינמית בהתאם למבנה הנתונים שהתקבל.

### 9.2 API / Controllers

אחראית על קבלת בקשות מהלקוח, קבלת פרמטרים וקבצי Excel, בדיקת תקינות בסיסית של הקלט, הפעלת השירות המתאים והחזרת תשובת HTTP. ה־Controllers לא יכילו לוגיקה עסקית מורכבת.

### 9.3 Business / Application Layer

אחראית על מימוש תהליכי העבודה, ניהול שיטות פרמיה ומדדים, תהליך קליטת קובץ, בדיקת התאמת מבנה הקובץ למדד, יצירת גרסת מבנה חדשה במקרה הצורך, שמירת היסטוריית הקליטה ושליפת נתונים עם סינון ומיון.

### 9.4 Domain Layer

אחראית על הגדרת המודלים המרכזיים, ישויות הדומיין והקשרים ביניהן, וכן כללים עסקיים בסיסיים. הלוגיקה המרכזית לא תהיה תלויה ב־Angular או ב־SQLite.

### 9.5 Data Access / Infrastructure Layer

אחראית על הגדרת הישויות במסד הנתונים, ביצוע פעולות CRUD, ביצוע שאילתות ושליפות, שמירת שיטות הפרמיה, המדדים, מבני הקבצים והנתונים, ומימוש הקשר ל־SQLite.

### 9.6 Interfaces והזרקת תלויות

המערכת תשתמש ב־Interfaces כדי להגדיר חוזים בין השכבות. ה־Controllers יעבדו מול ממשקי שירות, שכבת ה־Application תעבוד מול ממשקי Repository, ושכבת ה־Infrastructure תממש את הממשקים בפועל מול SQLite ו־Excel. המימושים יוזרקו באמצעות Dependency Injection.

ממשקים לדוגמה:

- `IPremiumMethodService`
- `IMetricService`
- `IImportService`
- `IExcelReader`
- `IPremiumMethodRepository`
- `IMetricRepository`
- `IImportRepository`
- `IDynamicDataRepository`

### 9.7 תהליך קליטת Excel

רכיב הקליטה יהיה אחראי על קריאת קובץ Excel, זיהוי שמות העמודות וסוגי הנתונים, המרת השורות למבנה גנרי, דיווח על שדות או נתונים לא תקינים והעברת הנתונים לשכבת ה־Application לצורך שמירה.

## 10. תכנון API

### 10.1 ניהול שיטות פרמיה

- `GET /api/premium-methods` – קבלת רשימת שיטות.
- `GET /api/premium-methods/{id}` – קבלת שיטה לפי מזהה.
- `POST /api/premium-methods` – יצירת שיטה.
- `PUT /api/premium-methods/{id}` – עדכון שיטה.

### 10.2 ניהול מדדים

- `GET /api/metrics?premiumMethodId={id}` – קבלת מדדים של שיטה.
- `GET /api/metrics/{id}` – קבלת מדד לפי מזהה.
- `POST /api/metrics` – יצירת מדד.
- `PUT /api/metrics/{id}` – עדכון מדד.
- `GET /api/metrics/{id}/fields` – קבלת שדות המדד.

### 10.3 קליטת קובץ

- `POST /api/imports` – העלאת קובץ Excel וקליטתו.
- הבקשה תכלול מזהה מדד, שנה, תקופת חישוב וקובץ Excel.
- התשובה תכלול סטטוס קליטה, מזהה קליטה, מספר רשומות שנקלטו והודעות שגיאה, אם קיימות.

### 10.4 היסטוריית קליטות

- `GET /api/imports?metricId={id}` – קבלת היסטוריית קליטות של מדד.
- `GET /api/imports/{id}` – קבלת פרטי קליטה.
- `GET /api/imports/{id}/errors` – קבלת שגיאות קליטה, אם קיימות.

### 10.5 שליפת נתוני מדד

- `GET /api/metrics/{id}/data` – קבלת נתוני המדד.
- הפרמטרים יאפשרו בחירת קליטה, סינון, מיון ועימוד.
- התשובה תכלול רשימת שדות, רשימת רשומות ומידע על העימוד.

### 10.6 שליפת שדות וסינונים אפשריים

- `GET /api/metrics/{id}/fields` – קבלת רשימת השדות.
- `GET /api/metrics/{id}/filters` – קבלת הסינונים האפשריים לפי סוגי השדות.

## 11. SQL ושליפות מרכזיות

### 11.1 שליפת שיטות פרמיה

שליפת רשימת שיטות הפרמיה, כולל אפשרות למיון בסיסי לפי מספר או תיאור.

```sql
SELECT
    Id,
    MethodNumber,
    Description,
    PremiumRate,
    CalculationPeriod
FROM PremiumMethods
ORDER BY MethodNumber;
```

### 11.2 שליפת מדדים

שליפת המדדים השייכים לשיטת פרמיה מסוימת.

```sql
SELECT
    Id,
    PremiumMethodId,
    Name,
    Description,
    SourceType,
    SourceName,
    IngestionFrequency
FROM Metrics
WHERE PremiumMethodId = @PremiumMethodId
ORDER BY Name;
```

### 11.3 שליפת היסטוריית קליטות

שליפת היסטוריית הקליטות עבור מדד, כולל שנה, תקופת חישוב, שם הקובץ, מועד הקליטה, סטטוס ומספר רשומות.

### 11.4 שליפת נתונים דינמיים

שליפת הרשומות והערכים הדינמיים לפי מזהה קליטה או מדד, תוך חיבור בין הקליטה, הרשומות, השדות והערכים.

### 11.5 שליפות עם סינון ומיון

השליפות יתמכו בסינון ובמיון לפי שדות דינמיים. הסינון והמיון יתבצעו בצד השרת, כדי לצמצם את כמות המידע המועברת ללקוח.

השאילתות יתמכו בעימוד לצורך עבודה עם כמויות מידע גדולות.

בשלב המימוש יוצגו SQL לדוגמה עבור השליפות המרכזיות.

## 12. חלופות לפתרון המידע הלא הומוגני

### 12.1 חלופה ראשונה

**שמירת הנתונים בפורמט JSON:** כל רשומת נתונים תישמר כאובייקט JSON, כאשר שמות השדות והערכים נמצאים בתוך האובייקט.

**יתרונות:**

- גמישות גבוהה.
- הוספת שדות אינה דורשת שינוי בסכמת ה־Database.
- מימוש פשוט יחסית.
- מתאים למבנים משתנים.

**חסרונות:**

- סינון ומיון לפי שדות דינמיים מורכבים יותר.
- ביצועים פחות צפויים בכמות מידע גדולה.
- קושי באכיפת טיפוסי נתונים.
- תלות ביכולות JSON של מסד הנתונים.
- שליפות SQL פחות ברורות.

### 12.2 חלופה שנייה

**מודל EAV:** הנתונים יישמרו כרשומות נפרדות, כאשר לכל ערך נשמרים מזהה הרשומה ומזהה השדה שאליו הוא שייך.

**יתרונות:**

- גמישות להוספת מדדים ושדות.
- אין צורך בשינוי מבנה הטבלאות.
- מבנה רלציוני ברור.
- ניתן לשמור סוגי נתונים ומטא־דאטה עבור השדות.
- מתאים לשמירת היסטוריית מבנים.

**חסרונות:**

- שליפות מורכבות יותר.
- סינון ומיון דורשים חיבורים נוספים.
- מספר הרשומות במסד הנתונים גדל.
- מימוש ותחזוקה מורכבים יותר מפתרון של טבלה קבועה.

### 12.3 הבחירה המועדפת והנימוקים

החלופות יושוו לפי יתרונות, חסרונות, מורכבות פיתוח, מורכבות תחזוקה, ביצועים, יכולת חיפוש וסינון וגמישות לשינויים עתידיים.

הפתרון המועדף הוא מודל EAV, משום שהוא מספק גמישות למבנים משתנים תוך שמירה על מבנה רלציוני, קשרים ברורים ואפשרות לשלוט במטא־דאטה של השדות.

הבחירה מתאימה לדרישות המבחן: הוספת מדד ללא שינוי משמעותי ב־Database, שינוי מבנה קובץ קיים, שמירת היסטוריית מבנים ושליפה, סינון ומיון בצד השרת.

## 13. תרחיש שינוי מבנה Excel

### 13.1 זיהוי המבנה החדש

בעת העלאת הקובץ המערכת תקרא את כותרות העמודות ותשווה אותן לגרסת המבנה הקיימת של מדד העובדים.

המערכת תזהה שהתווספו השדות `מנהל ישיר` ו־`מיקום`, ולכן תיצור גרסת מבנה חדשה.

### 13.2 צורך בשינוי במסד הנתונים

לא יידרש שינוי בטבלאות הקבועות של מסד הנתונים, משום שהנתונים נשמרים במבנה דינמי. יישמרו גרסת מבנה חדשה, שני השדות החדשים והערכים שלהם עבור הרשומות החדשות.

### 13.3 צורך בשינוי בקוד

לא יידרש שינוי בקוד העסקי או במבנה הטבלאות, כל עוד מנגנון הקליטה והצגת הנתונים הוא גנרי.

### 13.4 צורך בהגדרה במערכת

המערכת תאפשר לאשר את השדות שזוהו ולהגדיר אילו מהם רלוונטיים למדד. במקרה זה ניתן לסמן את `מנהל ישיר` ו־`מיקום` כשדות רלוונטיים.

### 13.5 שמירת היסטוריית המבנה

המערכת תשמור את המבנה הישן ואת המבנה החדש כגרסאות נפרדות, כאשר כל קליטה תקושר לגרסת המבנה המתאימה לה.

### 13.6 תאימות לנתונים ישנים

הנתונים הישנים יישארו משויכים לגרסת המבנה הישנה. בעת הצגתם יוצגו רק השדות שהיו קיימים באותה גרסה, והעמודות החדשות יוצגו כריקות או לא יוצגו עבור נתונים ישנים.

כך ניתן יהיה להציג גם נתונים ישנים וגם נתונים חדשים בלי לשנות את הנתונים שכבר נקלטו.

## 14. טכנולוגיות

### 14.1 צד לקוח

המערכת תמומש באמצעות **Angular**.

Angular ישמש עבור:

- בניית מסכי המערכת.
- ניהול טפסים באמצעות Reactive Forms.
- שליחת בקשות ל־Web API.
- הצגת נתונים דינמיים.
- סינון ומיון נתונים בצד הלקוח בהתאם לתוצאות מהשרת.

### 14.2 צד שרת

השרת ימומש באמצעות **.NET Core Web API**.

ה־API ישמש עבור:

- ניהול שיטות פרמיה.
- ניהול מדדים.
- קליטת קובצי Excel.
- ניהול היסטוריית קליטות.
- שליפת נתוני מדדים.
- שליפת שדות ואפשרויות סינון.

### 14.3 בסיס נתונים

המערכת תשתמש ב־**SQLite** במסגרת המבחן.

מבנה הטבלאות והקשרים יוגדר באופן רלציוני, כך שניתן יהיה להעביר את המערכת בעתיד ל־**SQL Server** ללא שינוי משמעותי במודל הנתונים או בלוגיקה העסקית.

## 15. תשובות לשאלות הנוספות

### 15.1 קבלת החלטה ארכיטקטונית

החלופות, ההשוואה והנימוקים לבחירת מודל EAV מופיעים בסעיף 12.

### 15.2 שינוי מבנה Excel

ההתייחסות לזיהוי מבנה חדש, שינויי Database וקוד, שמירת היסטוריה ותאימות לנתונים ישנים מופיעה בסעיף 13.
