# Environment Variables

The current backend uses a local SQLite database and does not require secrets or external API keys.

No `.env` file is required at this stage.

If environment variables are added later, document each variable here without storing real values.
